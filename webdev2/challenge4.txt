worldState was not being loaded when generating a new start. I had trouble passing the variable properly through multiple functions. It would be defined in the generateState function, but then it wasn't being passed into worldInfo using const worldInfo = `
World: ${gameState.world}